# --- Planilhas dos jogadores e modelo (DADOS PRIVADOS — nunca subir) ---
*.xlsx
engine/bets/

# Exceção: nenhum xlsx é necessário no repositório.
# O robô usa engine/data/bets_extracted.json (gerado por extract_bets.py)
# e engine/data/matches_reference.csv (catálogo dos jogos).

# --- macOS / editores ---
.DS_Store
.AppleDouble
.vscode/
.idea/
*.swp

# --- Python ---
__pycache__/
*.pyc
.pytest_cache/

# --- Logs e servidor local ---
*.log
landing-page/_serve.py

# --- Relatórios locais ---
engine/data/validation_report.txt

# Documentos pessoais / prints (não subir)
*.pdf
Screenshot*.png
*.png

# Dev-only preview tooling (Claude Code)
.claude/

engine/data/audit_report.txt

# Cache compartilhado dos eventos ESPN DENTRO de uma rodada do robô (efêmero — não persiste
# entre rodadas, cada run do GitHub Actions começa de um checkout novo)
engine/data/espn_events_cache.json

# A Rodada — artefatos gerados
a-rodada/preview.html
a-rodada/hoje.png

# Exceção: a caricatura oficial do Ricardo PODE ser commitada (apesar de *.png)
!a-rodada/ricardo.png

# Exceção: asset oficial da bola TRIONDA pode ser commitado (apesar de *.png)
!a-rodada/trionda.png
